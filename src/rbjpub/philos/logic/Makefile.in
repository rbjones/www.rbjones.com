# $Id: Makefile.in,v 1.3 2005/04/09 14:00:53 rbj Exp $

TOPSRCDIR=@top_srcdir@
RELSRCDIR=src/rbjpub/philos/logic
RELWEBDIR=rbjpub/philos/logic
WEBROOTDIR=../../../

PPBASEDB=zed
PPDB=phillog
PPDBDIR=
PPXML=
PPDOC=$(PPXML:.xml=.doc)
PPDOCDOC=
PPTEXDOC=$(PPDOCDOC)
PPTEXSML=$(PPTEXDOC:.doc=.sml)
PPLDS=$(PPXML:.xml=.lds) $(PPDOCDOC:.doc=.lds)
PPPPTH=
OPPTH=
# Plain ProofPower documents (not from XML)
PPPDOC=fabsem.doc
PPLDS=$(PPXML:.xml=.lds)
PPTH=$(PPPPTH) $(OPPTH)
PPTHTO=
#PPTHD=$(PPTH:.th=.thd)

XML005=$(PPXML)
HTMLTHLSi=$(HTMLTHLS:.html=-i.html)
HTMLTHLSm=$(HTMLTHLS:.html=-m.html)

TPDF=

ENTFILES=pp-symbol.ent

SRCDIRCPY=$(PPPDOC)
XLDPDIRCPY=$(ENTFILES)

include $(TOPSRCDIR)/build/common/vars.mk

WEBFILES=$(HTML005) $(HTMLTHLS) $(HTMLTHLSi) $(HTMLTHLSm)

include $(TOPSRCDIR)/src/common/rules.mk

# Paths

vpath %.in $(SRCDIR)
vpath %.xml $(SRCDIR)
vpath %.xsl $(XLCOMDIR)

# Rules

$(PPTH): $(PPDB).dbts 
	touch $@
