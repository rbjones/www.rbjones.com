# $Id: Makefile.in,v 1.5 2008/04/18 09:08:14 rbj01 Exp $

include @top_srcdir@/top_srcdir.mk

RELSRCDIR=src/rbjpub/www/column
RELWEBDIR=rbjpub/www/column

PPDB=
PPDBDIR=
PPXML=
PPDOC=$(PPXML:.xml=.doc)
# Plain ProofPower documents (not from XML)
PPPDOC=
KLUTEX=
PPLDS=$(PPXML:.xml=.lds)
PPTH=
PPTHD=$(PPTH:.th=.thd)

XML005=$(PPXML)
XML008=s060503.xml s060510.xml
#KLUTEX=pres001.tex

ENTFILES=pp-symbol.ent

SRCDIRCPY=$(PPPDOC) $(KLUTEX)
XLDPDIRCPY=$(ENTFILES)

include $(TOPSRCDIR)/build/common/vars.mk

WEBFILES=$(HTML005) $(PDF) $(HTML008)

include $(TOPSRCDIR)/src/common/rules.mk

# Paths

vpath %.in $(SRCDIR)
vpath %.xml $(SRCDIR)
vpath %.xsl $(XLCOMDIR)

# Rules

$(PPTH): $(PPDB).dbts 
	touch $@







