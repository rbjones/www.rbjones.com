# $Id: Makefile.in,v 1.7 2006/06/16 17:40:39 rbj01 Exp $

include @top_srcdir@/top_srcdir.mk

RELSRCDIR=src/rbjpub/www/papers
RELWEBDIR=rbjpub/www/papers

PPDB=
PPDBDIR=
PPXML=
PPDOC=$(PPXML:.xml=.doc)
# Plain ProofPower documents (not from XML)
PPPDOC=
KLUTEX=p000.tex p001.tex p002.tex p003.tex p004.tex p005.tex p006.tex p007.tex p008.tex p009.tex
PPLDS=$(PPXML:.xml=.lds)
PPTH=
PPTHD=$(PPTH:.th=.thd)

XML005=index.xml
XML007=$(PPXML)

ENTFILES=pp-symbol.ent
KLUFILES=rbjk.cls

SRCDIRCPY=$(PPPDOC) $(KLUTEX)
XLDPDIRCPY=$(ENTFILES)
COMDIRCPY=$(KLUFILES)

include $(TOPSRCDIR)/build/common/vars.mk

WEBFILES=$(HTML005) $(HTML007) $(PDF)

include $(TOPSRCDIR)/src/common/rules.mk

# Paths

vpath %.bib $(COMDIR)
vpath %.in $(SRCDIR)
vpath %.xml $(SRCDIR)
vpath %.xsl $(XLCOMDIR)

# Rules

$(PPTH): $(PPDB).dbts 
	touch $@
